# 2026-02-12

## 今日记录
- 完成 OpenClaw systemd 服务落地：`/etc/systemd/system/openclaw-gateway.service`，服务状态 `active (running)`，已开机自启。
- 服务代理配置按用户网络环境设置为 `192.168.5.3:7893`（HTTP/HTTPS/ALL_PROXY）。
- 核对系统时区：`Asia/Shanghai (CST, +0800)`。
- 初始化知识与记忆目录：`memory/`、`learnings/`，并创建 `memory/2026-02-12.md`、`learnings/README.md`、`MEMORY.md`、`memory/TEMPLATE.md`。
- 更新 `AGENTS.md`，加入“记忆管理/记录习惯/日志格式”规则。
- 创建每日学习总结 cron 任务（每天 00:00，Asia/Shanghai，isolated）：读取当日日志→生成 `learnings/YYYY-MM-DD.md`→更新 `MEMORY.md`→git 提交推送。
- 配置心跳：`agents.defaults.heartbeat.every = "30m"`，并重启网关生效。
- 完成本地 git 身份设置：`jzrm_clawbot <atpx4869love@gmail.com>`。

## 决策
- 日志体系采用三层：`memory/`（日常原始记录）→`learnings/`（学习提炼）→`MEMORY.md`（长期沉淀）。
- HEARTBEAT 同步命令分支统一用 `main`（避免 push 到不存在分支）。

## 待办
- 用户需在 GitHub 侧完成凭据认证后执行：`git push origin master:main`。
- 用户需尽快轮换已在聊天中暴露的 Telegram Bot Token 与 GitHub PAT。
- 可选：增加“每天首次对话自动创建当日日志”规则。

## 备注
- `gateway config.patch` 的旧 heartbeat 字段格式无效，当前版本应走 `agents.defaults.heartbeat.every`。
- [10:16] 执行一次“早报推送（天气+热点）”任务：默认城市上海（用户常驻地未记录），整理 6 条热点并通过 PushPlus（topic: 998877665544）发送成功；接口返回：`{"code":200,"msg":"执行成功","data":"0438ad97824346678813c290364a4299"}`。
- 完成 Git SSH 免密配置：已生成 `~/.ssh/id_ed25519`，远程改为 `git@github.com:atpx4869/Jzrm_openclaw.git`，认证测试通过；后续 push 走 SSH。
- 安装并启用可用 skills：`daily-briefing`、`google-search`、`mermaid-diagrams`、`firecrawl-search`、`bing-search`。
- 搜索链路验证：`firecrawl-search` 可用；`google-search` 持续 403（项目未获 Custom Search JSON API 权限）；临时策略为优先 Firecrawl/内置搜索。
- 早报城市已从“上海”改为“武汉”（更新 cron 任务 payload，次日 07:15 起生效）。
- 新增每日温和提醒任务：20:30 提醒复盘与待办（cron 已创建）。
- 搜索能力现状：Google Search 反复出现 403（项目未开通/无权限访问 Custom Search JSON API），已暂时切换为 Firecrawl 与内置搜索优先。
- 已收到并配置 API：Google API Key（轮替后仍403）、Firecrawl API Key（可用）、Tavily Key（先保留备用）写入 `/root/.openclaw/openclaw.env`。
- 已手动测试 PushPlus 补发：可成功发送；并按用户要求将天气+热点合并在同一条推送中。
- 用户将早报城市从“上海”改为“武汉”，相关 cron 任务已更新。
- 创建并初始化共享知识库：`~/.openclaw/shared-knowledge`，包含 `knowledge/` 多篇基础文档；已完成首个提交。
- 修复 HEARTBEAT 同步命令：workspace 推送改为 `git push origin master:main`；但 shared-knowledge 仍需用户绑定远程后才能自动 pull/push。
- 已将 shared-knowledge 连接到 GitHub 仓库 `atpx4869/Jzrm_openclaw_Knowledge` 并完成首次推送（master->master）。
- 用户将热点推送需求升级为：固定来源 `orz-ai/hot_news`，仅推 6 个榜单（百度、少数派、微博、知乎、36氪、吾爱破解）且各 10 条，时间为每天 07:00 与 17:00。
- 根据用户反馈“推送格式太丑”，已更新 cron 任务输出规范为 PushPlus `template=markdown`，按榜单分节并使用 `[标题](链接)`，避免大段裸链接与拥挤排版。
- [10:36] 执行 cron「热点推送（百度/少数派/微博/知乎/36氪/吾爱破解）」：使用 `hot_news` 数据源（`https://orz.ai/api/v1/dailynews`）抓取 6 个榜单各 10 条并整合单条 PushPlus 推送成功；返回：`{"code":200,"msg":"执行成功","data":"422190037c96410ba5f0d4e5705842e2"}`。
- [10:40] 再次执行同一热点推送任务并校验 Markdown 排版（分节+编号+链接）：PushPlus 发送成功；返回：`{"code":200,"msg":"执行成功","data":"111b0849b9094ab4aef1c7d581065a8e"}`。
